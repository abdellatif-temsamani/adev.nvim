# Adev-files

A minimal file manager included with `adev.nvim`, inspired by `mini.files` but
with a different workflow. It replaces netrw by default when the experimental
adev-files feature is enabled; set `replace_netrw = false` to opt out.
